# Database Configuration
DB_NAME=netzwaechter
DB_USER=postgres
DB_PASSWORD=CHANGE_ME_GENERATE_STRONG_PASSWORD
DB_PORT=5432

# Redis Configuration
REDIS_PASSWORD=CHANGE_ME_GENERATE_STRONG_PASSWORD
REDIS_PORT=6379

# Session Security (REQUIRED - 128+ characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=CHANGE_ME_MINIMUM_128_CHARACTERS

# Email Service
MAILSERVER_PASSWORD=your-smtp-password

# Application
NODE_ENV=production
BACKEND_PORT=5000
FRONTEND_PORT=80

# Connection Pool
DB_POOL_MIN=5
DB_POOL_MAX=20
DB_POOL_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=5000

# Production Scaling
BACKEND_REPLICAS=2
FRONTEND_REPLICAS=2

# Generate secrets with:
# openssl rand -base64 32  (for passwords)
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"  (for SESSION_SECRET)
